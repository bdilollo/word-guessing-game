// declare some variables, use querySelector
// eventListener on button
// function to start the game
// interval timer counting down
    // when timer hits 0, end game

// array of words to use for game
    // pick randomly from array to choose a word
// when game starts, blank spaces correlating to number of letters in a word
    // convert word to an array, # of blank spaces = array length
// replace corresponding blank spaces with a letter if the key is pushed
    // keydown eventListener 
        // iterate through each letter in word array, compare to key that was pressed

// counter to display wins and losses
    // kept in local storage